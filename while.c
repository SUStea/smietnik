#include <stdio.h>
#include <math.h>

int main()
{
int licznik=0;
while(licznik<10)
{
licznik+=1; //to samo co licznik = licznik + 1
printf("%d\n",licznik);
}
for(int i=0;i<10;i++)
{
printf("%d\n",i);
}
}
